%html
  %head
    %title Test | Joseph Hsu
    %link{:href => '/layout.css', :media => 'screen', :rel => 'stylesheet'}
  %body
    #container
      #header
        %h1 Joseph Hsu
        %div
          - if env['warden'].authenticated?
            authenticated
          - else
            not authenticated
      #content
        = yield
